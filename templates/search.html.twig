{% include 'header.html.twig' %}
{{ form(form) }}
{% if search_results is not null %}
    {% if search_results is empty %}
        <p>Geen inventarisnummers gevonden.</p>
    {% else %}
        <div class="search">
            <div class="search-header">
                <div class="search-cell">Thumbnail</div>
                <div class="search-cell">Inventarisnummer</div>
                <div class="search-cell">Titel</div>
                <div class="search-cell">Vervaardiger</div>
            </div>
            {% for result in search_results %}
                {% if result.thumbnail is empty %}
                    <div class="search-row" onclick="alert('Fout: inventarisnummer \'{{ result.inventory_number }}\' heeft geen afbeelding.')">
                        <div class="search-cell"></div>
                        <div class="search-cell">{{  result.inventory_number }}</div>
                        <div class="search-cell">{{  result.title }}</div>
                        <div class="search-cell">{{  result.creator }}</div>
                    </div>
                {% else %}
                    <a class="search-row" href="{{ path('create_blank', { id : result.id }) }}">
                        <div class="search-cell"><img src="{{  result.thumbnail }}" alt="" /></div>
                        <div class="search-cell">{{  result.inventory_number }}</div>
                        <div class="search-cell">{{  result.title }}</div>
                        <div class="search-cell">{{  result.creator }}</div>
                    </a>
                {% endif %}
            {% endfor %}
        </div>
    {% endif %}
{% endif %}
{% include 'footer.html.twig' %}
